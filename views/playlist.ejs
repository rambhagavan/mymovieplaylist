<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Movie List</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      margin: 20px;
    }
    .movies-container {
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
    }
    .movie {
      box-shadow: rgba(0, 0, 0, 0.05) 0px 0px 0px 1px, rgb(209, 213, 219) 0px 0px 0px 1px inset;
      background-color: #e1ecf4;
      margin-bottom: 20px;
      width: calc(33.333% - 20px); /* Three items per row with space between */
      box-sizing: border-box;
      /* padding: 10px; */
      height:20%;
    }
    .movie-poster {
      width: 180px;
      height: 180px;
      object-fit: cover; /* Ensures the image covers the container without distortion */
    }
  </style>
</head>
<body>
  <h1>Your Movie playlist</h1>
  <div class="movies-container">
    <% movies.forEach(movie => { %>
      <div class="movie">
        <img src="<%= movie.Poster %>" alt="<%= movie.Title %> Poster" style="width: 150px; height: 150px;">
        <p><strong>Genre:</strong> <%= movie.Genre %></p>
        <p><strong>Actors:</strong> <%= movie.Actors %></p>
        <p><strong>Plot:</strong> <%= movie.Plot %></p>
        <p><strong>Released:</strong> <%= movie.Released %></p>
        <p><strong>IMDB Rating:</strong> <%= movie.imdbRating %> (<%= movie.imdbVotes %> votes)</p>
        <% if (movie.Ratings && movie.Ratings.length > 0) { %>
          <p><strong>Ratings:</strong></p>
          <ul>
            <% movie.Ratings.forEach(function(rating) { %>
              <li><%= rating.Source %>: <%= rating.Value %></li>
            <% }); %>
          </ul>
        <% } %>
      </div>
    <% }); %>
  </div>
</body>
</html>
